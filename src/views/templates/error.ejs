<div style="max-width:500px;margin:60px auto;text-align:center;">
  <div class="card">
    <h2 style="color:var(--danger);">❌ <%= data.title %></h2>
    <p style="margin:16px 0;color:var(--text2);"><%= data.message %></p>
    <a href="<%= data.backUrl %>" class="btn btn-primary">← Go Back</a>
  </div>
</div>
